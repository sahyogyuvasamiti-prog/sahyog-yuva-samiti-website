Place brand assets here:
  logo.png   — the official Sahyog Yuva Samiti logo (used in the header and footer)
  mp-map.png — the Madhya Pradesh district map from the profile deck (shown on /presence)
Both are optional: the site renders a built-in fallback if a file is missing.
